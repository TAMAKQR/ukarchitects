# 🚀 Деплой UK Architects на Render.com

## Шаг 1: Подготовка Git репозитория

```bash
# Инициализируйте git (если еще не сделали)
cd "d:\UK Global"
git init

# Добавьте все файлы
git add .
git commit -m "Initial commit: UK Architects website"
```

## Шаг 2: Создайте GitHub репозиторий

1. Зайдите на https://github.com
2. Создайте новый репозиторий (например `uk-architects`)
3. **Не инициализируйте** его с README или .gitignore

```bash
# Привяжите локальный репозиторий к GitHub
git remote add origin https://github.com/ваш-username/uk-architects.git
git branch -M main
git push -u origin main
```

## Шаг 3: Регистрация на Render

1. Зайдите на https://render.com
2. Создайте аккаунт (можно через GitHub)
3. Подтвердите email

## Шаг 4: Создание Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Выберите **"Build and deploy from a Git repository"**
3. Подключите ваш GitHub аккаунт
4. Выберите репозиторий `uk-architects`
5. Нажмите **"Connect"**

## Шаг 5: Настройка сервиса

### Основные настройки:

- **Name:** `uk-architects` (или ваше название)
- **Region:** Выберите ближайший регион
- **Branch:** `main`
- **Root Directory:** `server`
- **Runtime:** `Node`
- **Build Command:** 
  ```
  npm install && npm run setup
  ```
- **Start Command:** 
  ```
  npm start
  ```

### Instance Type:

- **Free** - для тестирования (спит после 15 мин простоя)
- **Starter** ($7/мес) - для production (постоянно активен)

## Шаг 6: Настройка Cloudinary

⚠️ **ОБЯЗАТЕЛЬНЫЙ ШАГ!** Без этого загрузка изображений не будет работать.

1. Зарегистрируйтесь на https://cloudinary.com (бесплатно)
2. В Dashboard скопируйте:
   - Cloud Name
   - API Key
   - API Secret

📖 Подробная инструкция: [CLOUDINARY.md](CLOUDINARY.md)

## Шаг 7: Environment Variables

Нажмите **"Advanced"** → **"Add Environment Variable"** и добавьте:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `SESSION_SECRET` | Сгенерируйте случайную строку: https://randomkeygen.com |
| `PORT` | `3000` |
| `CLOUDINARY_CLOUD_NAME` | Ваш Cloud Name из Cloudinary |
| `CLOUDINARY_API_KEY` | Ваш API Key из Cloudinary |
| `CLOUDINARY_API_SECRET` | Ваш API Secret из Cloudinary |

## Шаг 8: Persistent Disk

✅ **ОПЦИОНАЛЬНО** (нужен только для БД, изображения теперь в Cloudinary!)

1. Прокрутите вниз до раздела **"Disks"**
2. Нажмите **"Add Disk"**
3. Настройте:
   - **Name:** `data`
   - **Mount Path:** `/opt/render/project/src/server`
   - **Size:** `1 GB` (минимум для Free tier)

## Шаг 8: Деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя (3-5 минут)
3. Следите за логами в реальном времени

### Что происходит:

```
- Клонирование репозитория
- npm install (установка зависимостей)
- npm run setup (инициализация БД + создание админа)
- npm start (запуск сервера)
```

## Шаг 9: Проверка работы

После успешного деплоя:

### Получите URL:

Ваш сайт будет доступен по адресу:
```
https://uk-architects-xyz.onrender.com
```

### Проверьте страницы:

- ✅ Главная: `https://your-app.onrender.com/`
- ✅ Проекты: `https://your-app.onrender.com/projects.html`
- ✅ Админка: `https://your-app.onrender.com/admin/login.html`

### Войдите в админку:

По умолчанию создается администратор:
- **Username:** `ukarchitects`
- **Email:** `ukarchitects.kg@gmail.com`
- **Password:** `ukarchitects`

⚠️ **СРАЗУ СМЕНИТЕ ПАРОЛЬ!**

## Шаг 10: Обновление API URLs (Опционально)

Если хотите захардкодить production URL:

```bash
# Локально на компьютере
cd "d:\UK Global\server"
set RENDER_URL=https://your-app.onrender.com
node update-api-urls.js

# Закоммитьте изменения
git add .
git commit -m "Update API URLs for production"
git push
```

Render автоматически задеплоит новую версию.

## Шаг 11: Настройка домена (Опционально)

### Для кастомного домена:

1. В настройках сервиса → **"Settings"** → **"Custom Domain"**
2. Добавьте ваш домен (например: `ukarchitects.kg`)
3. Настройте DNS записи у вашего регистратора:
   ```
   Type: CNAME
   Name: www
   Value: your-app.onrender.com
   ```

## 🔧 Обслуживание

### Обновление кода:

```bash
# Внесите изменения
git add .
git commit -m "Update feature X"
git push
```

Render автоматически задеплоит изменения!

### Просмотр логов:

В Dashboard → Ваш сервис → **"Logs"**

### Перезапуск сервера:

В Dashboard → Ваш сервис → **"Manual Deploy"** → **"Clear build cache & deploy"**

### Резервное копирование БД:

1. Dashboard → **"Shell"**
2. Скачайте database.db:
   ```bash
   cat database.db > /dev/stdout
   ```

## ⚠️ Важные замечания

### Free Tier ограничения:

- ✅ 750 часов работы в месяц (достаточно для 1 сервиса)
- ⚠️ Засыпает после 15 минут неактивности
- ⚠️ Первый запрос после сна занимает ~30-60 секунд
- ✅ 100 GB трафика в месяц
- ✅ Автоматический SSL сертификат

### Рекомендации для production:

1. 💰 Используйте платный план **Starter** ($7/мес)
2. 🔐 Смените все пароли администраторов
3. 📧 Настройте email для восстановления паролей
4. 📊 Добавьте мониторинг (UptimeRobot, Pingdom)
5. 💾 Регулярно делайте бэкапы БД

## 🆘 Проблемы и решения

### Сервис не запускается:

1. Проверьте логи в Render Dashboard
2. Убедитесь, что `rootDir: server` указан правильно
3. Проверьте, что все зависимости в package.json

### База данных теряется:

1. Убедитесь, что Persistent Disk добавлен
2. Проверьте Mount Path: `/opt/render/project/src/server`

### Долгий первый запрос:

Это нормально для Free tier - сервис "просыпается"

### CORS ошибки:

Добавьте в Environment Variables:
```
FRONTEND_URL=https://your-domain.com
```

## 📞 Поддержка

- Render Docs: https://render.com/docs
- Render Community: https://community.render.com
- GitHub Issues: Создайте issue в вашем репозитории

---

**Готово! Ваш сайт в облаке! 🎉**
